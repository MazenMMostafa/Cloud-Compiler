name: newbegin_compiler_server
description: Cloud compilation server for NewBegin Coding — compiles Arduino/ESP firmware via arduino-cli.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
